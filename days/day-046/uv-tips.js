/**
 * uv tips
 */

/**
 * repeat
 *
 * texture.repeat.set(u, v)
 * texture.wrapS = THREE.RepeatWrapping
 * texture.wrapT = THREE.RepeatWrapping
 */

/**
 * flipY
 *
 * some textures need flipY = false (e.g., when using glTF)
 */

/**
 * custom uv
 *
 * you can create your own uv attribute for custom geometry.
 */
