/**
 * day 46 - uv mapping
 */

/**
 * uv coordinates
 *
 * u = horizontal (0..1)
 * v = vertical (0..1)
 */

/**
 * textures are sampled using uv.
 * if uv go outside 0..1, wrapping rules apply.
 */

/**
 * default uv
 *
 * most built-in geometries provide UVs.
 * you can inspect them via:
 *   geometry.attributes.uv
 */
