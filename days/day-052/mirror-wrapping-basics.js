/**
 * day 52 - mirror wrapping
 */

/**
 * mirror wrapping
 *
 * texture.wrapS = THREE.MirroredRepeatWrapping
 * texture.wrapT = THREE.MirroredRepeatWrapping
 */

/**
 * how it works
 *
 * each repeated tile is flipped every other repeat.
 */

/**
 * combine with repeat
 *
 * texture.repeat.set(2, 2)
 */
