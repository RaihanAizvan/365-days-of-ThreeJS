/**
 * resize checklist
 */

/**
 * - update sizes
 * - update camera.aspect
 * - camera.updateProjectionMatrix()
 * - renderer.setSize(width, height)
 * - renderer.setPixelRatio(Math.min(devicePixelRatio, 2))
 */

/**
 * common bug: forgetting camera.updateProjectionMatrix()
 */
