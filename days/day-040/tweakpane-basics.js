/**
 * day 40 - tweakpane setup
 */

/**
 * install
 *
 * npm install tweakpane
 */

/**
 * basic usage
 */

// import { Pane } from 'tweakpane'
//
// const params = { speed: 1, color: '#4aa3ff' }
// const pane = new Pane()
// pane.addBinding(params, 'speed', { min: 0, max: 3, step: 0.01 })
// pane.addBinding(params, 'color')

/**
 * note
 *
 * update your scene inside the animation loop by reading params.
 */
