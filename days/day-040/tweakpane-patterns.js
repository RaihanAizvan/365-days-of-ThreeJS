/**
 * tweakpane patterns
 */

/**
 * pattern: folders
 *
 * pane.addFolder({ title: 'Lighting' })
 */

/**
 * pattern: buttons
 *
 * pane.addButton({ title: 'Reset' }).on('click', () => { ... })
 */

/**
 * pattern: separate params object
 *
 * const params = { intensity: 1.2, speed: 0.8 }
 * keep params separate from Three.js objects.
 */
