/**
 * texture tips
 */

/**
 * use power-of-two textures for best wrapping and mipmaps.
 */

/**
 * if textures look blurry:
 * - check minFilter / magFilter
 * - check pixel ratio
 */

/**
 * dispose textures when done:
 * texture.dispose()
 */
