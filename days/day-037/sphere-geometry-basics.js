/**
 * day 37 - sphere geometry
 */

/**
 * SphereGeometry(radius, widthSegments, heightSegments)
 *
 * widthSegments: horizontal divisions (longitude)
 * heightSegments: vertical divisions (latitude)
 */

/**
 * segment tradeoff
 *
 * low segments => faceted look, fewer triangles
 * high segments => smoother look, more triangles
 */

/**
 * use cases
 *
 * - low segments for distant objects or stylized looks
 * - higher segments for close-up smooth spheres
 */
