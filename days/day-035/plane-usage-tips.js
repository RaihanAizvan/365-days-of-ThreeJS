/**
 * plane usage tips
 */

/**
 * segments
 *
 * more segments => more triangles
 * needed when you deform a plane (terrain, waves).
 */

/**
 * double-sided
 *
 * planes are one-sided by default (front side).
 * if you need both sides:
 *   material.side = THREE.DoubleSide
 */
