/**
 * day 38 - geometry attributes
 */

/**
 * BufferGeometry attributes
 *
 * geometry.attributes is a map of BufferAttributes.
 * common keys:
 * - position
 * - normal
 * - uv
 * - color
 */

/**
 * attribute count
 *
 * attribute.count = number of vertices
 *
 * for position:
 * count = positions.length / 3
 */

/**
 * updating attributes
 *
 * if you modify attribute.array, set:
 * - attribute.needsUpdate = true
 */

/**
 * debugging
 *
 * console.log(geometry.attributes)
 */
