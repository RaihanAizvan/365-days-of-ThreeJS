/**
 * attribute update notes
 */

/**
 * if you update positions:
 * - set needsUpdate
 * - recompute normals if lighting looks wrong
 */

/**
 * for frequent updates:
 * - attribute.setUsage(THREE.DynamicDrawUsage)
 */

/**
 * important:
 * attributes are typed arrays, so update values in-place.
 */
