/**
 * attribute usage notes
 */

/**
 * vertex colors
 *
 * to use colors you must:
 * - add a 'color' attribute
 * - set material.vertexColors = true
 */

/**
 * dynamic draw usage
 *
 * for frequently updated attributes:
 *   attribute.setUsage(THREE.DynamicDrawUsage)
 */

/**
 * attribute counts
 *
 * positions.length / itemSize = vertex count
 */
