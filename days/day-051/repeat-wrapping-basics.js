/**
 * day 51 - repeat wrapping
 */

/**
 * wrap modes
 *
 * texture.wrapS = THREE.RepeatWrapping
 * texture.wrapT = THREE.RepeatWrapping
 */

/**
 * tiling
 *
 * texture.repeat.set(u, v)
 */

/**
 * offset
 *
 * texture.offset.set(u, v)
 * can be animated to scroll textures.
 */
