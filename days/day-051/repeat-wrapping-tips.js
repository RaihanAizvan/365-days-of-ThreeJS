/**
 * repeat wrapping tips
 */

/**
 * to repeat, texture must be power-of-two if you want mipmaps.
 */

/**
 * clamp-to-edge is the default wrap mode.
 */

/**
 * mirror wrapping is another option:
 *   texture.wrapS = THREE.MirroredRepeatWrapping
 */
