/**
 * MeshStandardMaterial presets for common materials
 */

/**
 * plastic (non-metal, medium roughness)
 *
 * new THREE.MeshStandardMaterial({
 *   color: 0xff0000,
 *   metalness: 0.0,
 *   roughness: 0.4
 * })
 */

/**
 * polished metal (full metal, low roughness)
 *
 * new THREE.MeshStandardMaterial({
 *   color: 0xcccccc,
 *   metalness: 1.0,
 *   roughness: 0.1
 * })
 */

/**
 * brushed metal (full metal, medium roughness)
 *
 * new THREE.MeshStandardMaterial({
 *   color: 0xaaaaaa,
 *   metalness: 1.0,
 *   roughness: 0.4
 * })
 */

/**
 * gold (full metal, specific color)
 *
 * new THREE.MeshStandardMaterial({
 *   color: 0xffd700,
 *   metalness: 1.0,
 *   roughness: 0.2
 * })
 */

/**
 * wood (non-metal, high roughness)
 *
 * new THREE.MeshStandardMaterial({
 *   color: 0x8b4513,
 *   metalness: 0.0,
 *   roughness: 0.8
 * })
 */

/**
 * glass-like (transparent, low roughness)
 *
 * new THREE.MeshStandardMaterial({
 *   color: 0xffffff,
 *   metalness: 0.0,
 *   roughness: 0.1,
 *   transparent: true,
 *   opacity: 0.5
 * })
 */

/**
 * rubber (non-metal, high roughness)
 *
 * new THREE.MeshStandardMaterial({
 *   color: 0x333333,
 *   metalness: 0.0,
 *   roughness: 0.9
 * })
 */

/**
 * ceramic (non-metal, low-medium roughness)
 *
 * new THREE.MeshStandardMaterial({
 *   color: 0xffffff,
 *   metalness: 0.0,
 *   roughness: 0.3
 * })
 */
