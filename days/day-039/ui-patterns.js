/**
 * debugging ui patterns
 */

/**
 * pattern: state object
 *
 * const params = {
 *   speed: 1,
 *   intensity: 2,
 *   color: '#ffcc00'
 * }
 */

/**
 * pattern: bind inputs
 *
 * input.addEventListener('input', () => {
 *   params.speed = Number(input.value)
 * })
 */

/**
 * pattern: display values
 *
 * use text elements to show the active value.
 */
