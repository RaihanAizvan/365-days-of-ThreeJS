/**
 * mipmapping tips
 */

/**
 * if you set minFilter to a mipmap filter,
 * but the texture is not power-of-two, mipmaps will be disabled.
 */

/**
 * for pixel-art textures:
 * - use NearestFilter
 * - disable mipmaps if you want crisp pixels
 */
