/**
 * day 47 - color management
 */

/**
 * sRGB vs linear
 *
 * - textures that represent color should be in sRGB
 * - data textures should remain linear
 */

/**
 * three js defaults
 *
 * renderer.outputColorSpace = THREE.SRGBColorSpace
 * texture.colorSpace = THREE.SRGBColorSpace (for color textures)
 */

/**
 * if colors look too dark/light, check color space first.
 */
