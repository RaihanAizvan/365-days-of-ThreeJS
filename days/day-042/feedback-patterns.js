/**
 * feedback patterns
 */

/**
 * pattern: state machine
 *
 * const state = { hovered: false, pressed: false }
 * update visuals in the loop based on state.
 */

/**
 * pattern: lerp for smooth transitions
 *
 * current = lerp(current, target, 0.1)
 */

/**
 * pattern: time-based pulse
 *
 * scale = 1 + Math.sin(time * 6) * 0.05
 */
