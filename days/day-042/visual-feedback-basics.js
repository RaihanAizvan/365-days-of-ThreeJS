/**
 * day 42 - visual feedback
 */

/**
 * feedback techniques
 *
 * - color shift (hover/active)
 * - scale pulse (press)
 * - subtle motion (alive state)
 * - glow or outline (focus)
 */

/**
 * keep it subtle
 *
 * too much motion becomes noise.
 * feedback should be immediate and readable.
 */

/**
 * state-driven
 *
 * feedback should reflect state (hovered, pressed, selected).
 */
