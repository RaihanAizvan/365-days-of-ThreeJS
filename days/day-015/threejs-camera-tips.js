/**
 * camera tips (three js)
 */

/**
 * lookAt
 *
 * camera.lookAt(x, y, z) rotates the camera so it points at a target.
 *
 * note: lookAt affects rotation, not position.
 */

/**
 * common starting values
 *
 * - camera position: (0, 2, 5)
 * - near: 0.1
 * - far: 100
 * - fov: 50-75
 */

/**
 * debug helpers
 *
 * - AxesHelper / GridHelper for orientation
 * - CameraHelper(camera) to visualize frustum
 */

/**
 * remember: the camera is just another Object3D
 *
 * you can parent it to a group, animate it, or attach it to a player.
 */
