/**
 * loading manager tips
 */

/**
 * show a progress bar
 * - update width: loaded / total
 */

/**
 * handle errors gracefully
 * - show a fallback texture
 */

/**
 * avoid blocking render loop
 * - keep rendering while loading
 */
