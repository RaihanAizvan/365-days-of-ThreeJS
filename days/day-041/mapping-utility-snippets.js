/**
 * mapping utility snippets
 */

/**
 * lerp
 */

// const lerp = (a, b, t) => a + (b - a) * t

/**
 * clamp
 */

// const clamp = (v, min, max) => Math.min(Math.max(v, min), max)

/**
 * mapRange
 */

// const mapRange = (v, inMin, inMax, outMin, outMax) => {
//   const t = (v - inMin) / (inMax - inMin)
//   return outMin + (outMax - outMin) * t
// }
