/**
 * day 50 - anisotropy theory
 */

/**
 * anisotropy
 *
 * increases texture sampling quality when viewing at steep angles.
 * more samples along the direction of distortion.
 */

/**
 * usage
 *
 * const max = renderer.capabilities.getMaxAnisotropy()
 * texture.anisotropy = Math.min(8, max)
 */

/**
 * cost
 *
 * higher anisotropy = better quality but more GPU cost.
 */
