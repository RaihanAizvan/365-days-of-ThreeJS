/**
 * day 49 - filtering modes
 */

/**
 * minification filters
 *
 * used when texture is smaller than a pixel on screen.
 * common:
 * - LinearMipMapLinearFilter (default)
 * - LinearFilter (no mipmaps)
 * - NearestFilter (no mipmaps)
 */

/**
 * magnification filters
 *
 * used when texture is larger than a pixel on screen.
 * only two options:
 * - LinearFilter
 * - NearestFilter
 */

/**
 * pixel art
 *
 * use NearestFilter for crisp pixels.
 */
