/**
 * day 36 - box geometry
 */

/**
 * BoxGeometry(width, height, depth, widthSegments, heightSegments, depthSegments)
 */

/**
 * segments
 *
 * segments subdivide faces into more triangles.
 * useful for:
 * - vertex displacement (waving boxes)
 * - smooth lighting changes after deformation
 */

/**
 * default box
 *
 * new THREE.BoxGeometry(1, 1, 1)
 */
